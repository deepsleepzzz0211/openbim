# 07: BVH 拾取 + 构件区间表

**What to build:** 用户在大模型上点击/框选构件得到亚毫秒响应：拾取经加速结构命中三角形后，用块内「构件→三角形区间」表二分定位 GUID，不再遍历全部三角形；剖切/隔离激活时拾取结果与可见性一致（拾不到看不见的东西）。

**Blocked by:** 06

**Status:** implemented (2026-09-20), awaiting review；拾取延迟对比留档待人工补拍（自动化浏览器操作被取消）

- [x] 已加载块建立 BVH 加速结构（构建在 worker 中完成，不阻塞交互线程），结果可缓存复用
- [x] 命中三角形经区间表二分映射到构件（映射表数据来自 05 的 manifest）
- [x] 框选/多选走场景级加速结构；隔离、楼层隐藏、剖切状态下拾取过滤正确
- [ ] 留档：最大样例上拾取延迟对比（改造前/后，取多次中位数）
- [x] 回归：既有拾取查属性、测量、BCF 视点选中构件行为不变
