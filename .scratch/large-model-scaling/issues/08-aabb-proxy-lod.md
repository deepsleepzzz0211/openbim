# 08: AABB 代理 LOD 层

**What to build:** 用户打开超大模型时先看到全模型的盒体概览（楼栋/楼层轮廓），可立即做选层、总览、定位，远处不拉真几何，走近才细化——总览首屏目标 <2s。

**Blocked by:** 06

**Status:** implemented (2026-09-20), awaiting review；浏览器留档待人工补拍（自动化浏览器操作被取消）

- [x] 转换产出全模型代理层（按构件或按块的 AABB 盒体集合，一次小体积加载）（无需改转换器：Element.bboxJson 已落库，新增 GET /versions/:id/proxy 一次性返回全模型盒体 JSON）
- [x] Viewer 三态显示模式（仅代理 / 自动 LOD / 全几何），默认自动：视距驱动块加载
- [x] 选层/框选在代理层即可完成并即时反馈（代理命中经 instanceId 映射回构件，与真实网格统一走 selectionAt）
- [ ] 留档：最大真实样例首屏可见时间（代理先行 vs 现状全量）
- [x] 代理层与既有楼层树、隔离视图行为一致（代理可见性复用同一 storeyKey 集合，楼层树/隔离无需改动）
