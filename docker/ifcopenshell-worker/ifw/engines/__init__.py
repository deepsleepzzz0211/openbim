"""Triangulation engines for the native conversion worker (ticket 09).

`ifcopenshell_engine` is the production engine (IfcOpenShell, separate process
=> LGPL boundary). `stub` produces synthetic documents for unit tests and for
`IFW_ENGINE=stub` dev runs of the container protocol.
"""
